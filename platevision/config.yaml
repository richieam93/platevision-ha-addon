name: "PlateVision ProTraffic People ROI Mouse Fix"
slug: platevision
description: PlateVision ProTraffic People mit einheitlichem Straßen-ROI, Personenanalyse, Modell-Upload, Kennzeichen-Erkennung, Statistik, Suche, Dashboard, RTSP und erweiterten Original-Einstellungen
version: "0.8.8"
arch:
  - amd64
startup: services
boot: auto
init: false

ports:
  5000/tcp: 8087
ports_description:
  5000/tcp: PlateVision Web UI

webui: "http://[HOST]:[PORT:8087]"
