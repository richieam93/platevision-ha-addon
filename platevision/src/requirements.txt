# PlateVision - Python requirements
# UTF-8 encoded. Use opencv-python-headless for Home Assistant / Docker.
# Major versions are bounded so an add-on rebuild does not silently pull a
# breaking OCR/YOLO/OpenCV generation after an update.
Flask>=3.0,<4
Flask-SocketIO>=5.3,<6
Werkzeug>=3.0,<4
python-socketio>=5.11,<6
python-engineio>=4.9,<5
eventlet>=0.35,<1
gunicorn>=21.2,<24
opencv-python-headless>=4.8,<5
ultralytics>=8.0,<9
numpy>=1.24,<3
Pillow>=10.0,<12
easyocr>=1.7,<2
torch>=2.0,<3
torchvision>=0.15,<1
python-dotenv>=1.0,<2

pytesseract>=0.3,<1

# Zusätzliche OCR-Engine für Kamera-/Kennzeichenbilder
paddleocr>=3.0,<4
paddlepaddle>=3.0,<4
