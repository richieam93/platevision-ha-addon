automation:
  - alias: "PlateVision - Neue Erkennung"
    trigger:
      - platform: state
        entity_id: sensor.platevision_last_plate
    condition:
      - condition: template
        value_template: "{{ trigger.to_state.state != trigger.from_state.state }}"
      - condition: template
        value_template: "{{ trigger.to_state.state | length > 2 }}"
    action:
      - service: notify.mobile_app
        data:
          title: "🚗 Kennzeichen erkannt"
          message: "{{ states('sensor.platevision_last_plate') }} ({{ states('sensor.platevision_vehicle_type') }}, {{ states('sensor.platevision_vehicle_color') }})"
          data:
            image: /api/camera_proxy/camera.platevision_vehicle_image